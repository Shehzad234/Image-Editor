import 'package:flutter/material.dart';

List<ColorFilter> listOfFilters = [
  ColorFilter.mode(Colors.black.withOpacity(0.0), BlendMode.color),
  const ColorFilter.mode(Colors.black, BlendMode.color),
  const ColorFilter.mode(Colors.green, BlendMode.color),
  const ColorFilter.mode(Colors.blue, BlendMode.color),
  const ColorFilter.mode(Colors.pink, BlendMode.color),
  const ColorFilter.mode(Colors.purple, BlendMode.color),
  const ColorFilter.mode(Colors.grey, BlendMode.color),
  const ColorFilter.mode(Colors.brown, BlendMode.color),
  const ColorFilter.mode(Colors.orange, BlendMode.color),
];
